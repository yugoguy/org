# Experiment Notebooks

Jupyter notebooks reproducing all reported results. Notebook filenames indicate method, task, and seed.

## Planar Arm
Runs across seeds 42, 123, 456. All four task variants (linear/nonlinear × deterministic/uncertain) are evaluated within each notebook. Some methods run all seeds in a single notebook; LVE + BehavePred uses separate notebooks per seed.

## Ant-v5
Runs across seeds 42, 123. Each method has a separate notebook per seed.

## Additional
- `Images&Analysis.ipynb` — Latent space visualizations (PCA, t-SNE, UMAP), Spearman correlation analysis, and figure generation.
